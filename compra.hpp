#include <string>
using namespace std;

class Compra{
    private:
        string id, idLoja, nomeProduto;
        int qtdProduto, numParcelas, qtdParcelasPagas = 0;
        double valorUnitario;
    
    public:
    
};